generator client {
    provider     = "prisma-client"
    output       = "../generated"
    moduleFormat = "esm"
    runtime      = "nodejs"
}

datasource db {
    provider = "postgresql"
}

model users {
    user_id String  @id @default(uuid()) @db.Uuid
    email   String? @unique @db.VarChar(255)
}
