{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@exness-clone/db": "workspace:*",
    "@types/jsonwebtoken": "^9.0.10",
    "cors": "^2.8.5",
    "dotenv": "catalog:",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "resend": "^6.5.2",
    "zod": "catalog:"
  },
  "trustedDependencies": [
    "supabase"
  ],
  "devDependencies": {
    "typescript": "catalog:",
    "@types/express": "^5.0.1",
    "@types/cors": "^2.8.17",
    "tsx": "^4.19.2",
    "@types/node": "catalog:",
    "tsdown": "^0.16.5",
    "@exness-clone/config": "workspace:*"
  }
}
